server:
  port: 8080

spring:
  application:
    name: stock-easy

management:
  endpoints:
    web:
      exposure:
        include: "health,info"

# TODO(config): Configure database connection
# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/stock_easy
#     username: user
#     password: password

# TODO(config): Configure logging levels
# logging:
#   level:
#     com.example.stockeasy: DEBUG

# TODO(config): Configure CORS
# server:
#   cors:
#     allowed-origins: "http://localhost:3000"
#     allowed-methods: "GET,POST,PUT,DELETE"